PowerShell

```
pip install requests pydouyin -i http://mirrors.aliyun.com/pypi/simple/ --trusted-host mirrors.aliyun.com
```

**为什么这招管用？** 加上 `--trusted-host` 和使用 `http://` 开头的地址后，`pip` 就会绕过底层那层烦人的 SSL 证书握手（也就是报错里那个 `_ssl.c`），直接把包裹拿回来。