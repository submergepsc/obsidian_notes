---
id: 20260511-c9f2-plot3-font32
name: Plot3 Font Size 32
slug: plot3-font32
cwd: /home/loviya/code/RWAExpResults
summary: 调整 plot3：POS 保持时间序列图，POW 改为五个纵向堆叠的水平 boxplot 面板。
tags:
  - RWAExpResults
  - plot3
  - figure
priority: normal
---

# Plot3 Font Size 32

## 当前快照

- 状态: 已完成
- 目标: 保持 POS 为吞吐量时间序列面板，并将 POW 改为五个纵向堆叠的水平 boxplot 面板。
- 阻塞: 无。
- 下一步: 无。
- 更新时间: 2026-05-12 00:11:03 +0800

## 关键结果

- 已更新 `AXIS_LABEL_SIZE`, `TICK_LABEL_SIZE`, and `LEGEND_FONT_SIZE` to `32` in `plot_3_throught.py`.
- `ANNOTATION_FONT_SIZE` was already `32` and remains unchanged.
- Replaced the POW inset hardcoded tick label size `10` 带 `TICK_LABEL_SIZE`, so inset ticks also use `32`.
- Regenerated `figures/03_throughput/throughput_stability_pos.pdf` and `figures/03_throughput/throughput_stability_pow.pdf`.
- Rendered temporary PNG checks under `/tmp/throughput_stability_*_font32_check.png`; POW inset is visually crowded 因为 every text element is now 32.
- 后续 cleanup kept the main plot3 typography at 32, but restored compact POW inset ticks/title so the zoom box remains readable.
- 已修复 the POW log-axis tick locators so minor ticks no longer create overlapping labels.
- 已删除 the redundant POW boxplot legend 因为 the category labels already identify protocols and the legend overlapped the zoom inset.
- Increased plot3 margins so POS/POW y-axis labels are not clipped after regeneration.
- 后续 POS restore added POS-specific compact font/line constants and regenerated only `figures/03_throughput/throughput_stability_pos.pdf`.
- 后续 correction removed the temporary POW boxplot branch entirely and restored the original-style vertical broken-axis throughput time series.
- Final correction kept POW as a boxplot, removed its local zoom inset, removed log scaling, and split the five protocols into five horizontal panels.
- Ran the full `plot_3_throught.py` script 带 the project virtualenv Python and found that `main.tex` references `figure/...` while the plotting script writes `figures/...`.
- 已新增 a repo-local symlink `figure -> figures` so the paper's existing include paths resolve to the generated figures.
- Rebuilt `main.pdf`; the PDF was written, but `latexmk` still 返回 nonzero 因为 of unrelated missing bibliography and old missing figure assets.
- Final POW layout uses horizontal boxplots stacked vertically, one protocol per row, 带 focused per-row x-axis ticks so each boxplot occupies most of its row.
- Increased POW stacked-boxplot typography to 32 for protocol labels and tick labels, then increased each row from three x-axis ticks to four.

## 命令

- `python3 plot_3_throught.py`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pow.pdf /tmp/throughput_stability_pow_font32_check`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pos.pdf /tmp/throughput_stability_pos_font32_check`
- `MPLCONFIGDIR=/tmp/matplotlib-cache python3 plot_3_throught.py`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pow.pdf /tmp/throughput_stability_pow_plot3_final`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pos.pdf /tmp/throughput_stability_pos_plot3_final`
- `MPLCONFIGDIR=/tmp/matplotlib-cache python3 - <<'PY' ... plot_throughput_stability('pos', out_dir) ... PY`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pos.pdf /tmp/throughput_stability_pos_restore_final`
- `MPLCONFIGDIR=/tmp/matplotlib-cache python3 - <<'PY' ... plot_throughput_stability('pow', out_dir) ... PY`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pow.pdf /tmp/throughput_stability_pow_timeseries_restore`
- `rg -n "boxplot|箱线图|inset_axes|Zoom|minute_bin" plot_3_throught.py`
- `MPLCONFIGDIR=/tmp/matplotlib-cache python3 - <<'PY' ... plot_throughput_stability('pow', out_dir) ... PY`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pow.pdf /tmp/throughput_stability_pow_5panels`
- `rg -n "LogLocator|NullFormatter|inset_axes|Zoom|set_yscale|场景 A: PoW|plt\\.subplots\\(|wspace|boxplot" plot_3_throught.py`
- `MPLCONFIGDIR=/tmp/matplotlib-cache /home/loviya/code/RWAExpResults/.venv/bin/python plot_3_throught.py`
- `ln -s figures figure`
- `latexmk -pdf -interaction=nonstopmode main.tex`
- `MPLCONFIGDIR=/tmp/matplotlib-cache /home/loviya/code/RWAExpResults/.venv/bin/python plot_3_throught.py`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pow.pdf /tmp/plot3_pow_vertical_horizontal_boxplots`
- `pdftoppm -png -singlefile figures/03_throughput/throughput_stability_pow.pdf /tmp/plot3_pow_4ticks`

## Plot3 POW Boxplot Needed A Readable Zoom

- 更新时间: 2026-05-11 23:01:26 +0800
- 工作目录: `/home/loviya/code/RWAExpResults`
- 来源指令: `继续之前的工作流,plot3的地方改一下`
- 问题:
  - After plot3 text was changed to 32, the POW throughput boxplot had crowded log-axis labels, an oversized zoom inset, and a legend overlapping the inset.
  - POS and POW y-axis titles risked being clipped 带 the larger text.
- 改进:
  - 已新增 dedicated POW inset font constants for compact tick/title text.
  - Switched the POW log axis to `LogLocator` plus `NullFormatter` for minor tick labels.
  - Skipped the redundant POW legend and enlarged plot margins.
- 结果:
  - Regenerated `figures/03_throughput/throughput_stability_pos.pdf` and `figures/03_throughput/throughput_stability_pow.pdf`.
  - Render-checked final PNGs at `/tmp/throughput_stability_pos_plot3_final.png` and `/tmp/throughput_stability_pow_plot3_final.png`.
- 下一步:
  - 无

## Plot3 POS Should Return To Earlier Compact Style

- 更新时间: 2026-05-11 23:12:25 +0800
- 工作目录: `/home/loviya/code/RWAExpResults`
- 来源指令: `把这个plot3的pos部分的代码改回去之前的途中这个`
- 问题:
  - The POS throughput panel was still inheriting the global 32-size plot3 typography, making it much larger than the referenced earlier figure.
- 改进:
  - 已新增 POS-only style constants for axis labels, tick labels, legend, annotation text, line widths, vertical completion lines, and annotation box padding.
  - Restored the POS x-axis label to `ax1.set_xlabel("Time (min)")` so it sits near the axis like the earlier figure 而不是 使用 the global bottom `fig.text`.
  - Kept the POW branch untouched during regeneration by calling only `plot_throughput_stability('pos', out_dir)`.
- 结果:
  - Regenerated `figures/03_throughput/throughput_stability_pos.pdf`.
  - Render-checked `/tmp/throughput_stability_pos_restore_final.png`; the POS panel now matches the compact earlier style shown by the user.
- 下一步:
  - 无

## Plot3 POW Should Not Be A Boxplot

- 更新时间: 2026-05-11 23:15:36 +0800
- 工作目录: `/home/loviya/code/RWAExpResults`
- 来源指令: `你搞错了,我要你退回到最开始绘制这种不是箱线图的时候`
- 问题:
  - The previous change misunderstood the request and kept the POW throughput panel as a boxplot.
  - The intended target was the earlier throughput-over-time plot, not a distribution boxplot.
- 改进:
  - Replaced the POW `boxplot` branch in `plot_3_throught.py` 带 the earlier vertical broken-axis line chart logic.
  - Regenerated only `figures/03_throughput/throughput_stability_pow.pdf`.
- 结果:
  - `plot_3_throught.py` no longer contains `boxplot`, `minute_bin`, `Zoom`, or `inset_axes` logic.
  - Render check at `/tmp/throughput_stability_pow_timeseries_restore.png` confirms the POW panel is now a time-series plot.
- 下一步:
  - 无

## POW Boxplot Should Be Five Side-By-Side Panels

- 更新时间: 2026-05-11 23:19:10 +0800
- 工作目录: `/home/loviya/code/RWAExpResults`
- 来源指令: `pow保持箱线图` and `进行这样的修改,删掉箱线图的局部放大部分,然后把这几个换成五个横向并列的图,不需要log对数`
- 问题:
  - The corrected direction is that POW should remain a boxplot, but the previous single-axis version depended on log scaling and a zoom inset.
- 改进:
  - 已删除 the POW zoom inset and all log-axis handling from `plot_3_throught.py`.
  - Reworked POW boxplot rendering into five horizontal subplots, one protocol per panel, each 带 its own linear y-axis.
  - Kept POS as the compact time-series panel.
- 结果:
  - Regenerated `figures/03_throughput/throughput_stability_pow.pdf`.
  - Render-checked `/tmp/throughput_stability_pow_5panels.png`; POW now shows five side-by-side non-log boxplot panels.
- 下一步:
  - 无

## Plot3 Output Path Needed To Match Main Tex

- 更新时间: 2026-05-11 23:25:27 +0800
- 工作目录: `/home/loviya/code/RWAExpResults`
- 来源指令: `你运行一下你修改的plot3,为什么我没有看到你的修改`
- 问题:
  - Running `plot_3_throught.py` generated updated PDFs under `figures/03_throughput/`, but `main.tex` includes `figure/03_throughput/...`.
  - The repository had no `figure/` directory before this check, so the paper path did not point at the script output.
- 改进:
  - Ran the full plot3 script 使用 `/home/loviya/code/RWAExpResults/.venv/bin/python`.
  - 已创建 a local symlink `figure -> figures` so existing LaTeX include paths resolve to the generated assets.
  - Rebuilt `main.pdf`.
- 结果:
  - `figures/03_throughput/throughput_stability_pow.pdf` and `figure/03_throughput/throughput_stability_pow.pdf` now resolve to the same file.
  - `main.pdf` was rewritten and included the plot3 throughput PDFs.
  - `latexmk` still reported unrelated missing `reference.bib`, `score_distribution.pdf`, `score_vs_max_block_num.pdf`, `c_vs_alpha.pdf`, and `01_breakdown` PDFs.
- 下一步:
  - If a clean LaTeX build is required, restore the missing bibliography and old figure assets or remove those references.

## POW Boxplots Should Be Horizontal And Vertically Stacked

- 更新时间: 2026-05-11 23:35:53 +0800
- 工作目录: `/home/loviya/code/RWAExpResults`
- 来源指令: `你搞错了,我是说每个方案都是横过来的,整体就是竖着排列的`
- 问题:
  - The previous POW boxplot layout made each box horizontal but arranged all protocols side by side, contrary to the requested vertical stacking.
- 改进:
  - 已修改 the POW subplot grid to five rows and one column.
  - Kept each protocol's boxplot horizontal 带 `vert=False`.
  - Focused each row's x-axis on the protocol's 10%-90% range and hid fliers, so the visible boxplot body takes most of the row width.
- 结果:
  - Regenerated `figures/03_throughput/throughput_stability_pow.pdf`.
  - Render-checked `/tmp/plot3_pow_vertical_horizontal_boxplots.png`; the figure now has five vertically stacked horizontal boxplots.
- 下一步:
  - 无

## POW Stacked Boxplot Font And Ticks Should Be Larger

- 更新时间: 2026-05-12 00:11:03 +0800
- 工作目录: `/home/loviya/code/RWAExpResults`
- 来源指令: `很好了,把字体都调整到32差不多了` and `我觉得刻度可以增加一点`
- 问题:
  - The POW stacked horizontal boxplot still used small protocol labels and sparse three-point x-axis ticks.
- 改进:
  - Set POW protocol labels and x-axis tick labels to 32.
  - Increased each POW row from three x-axis ticks to four evenly spaced ticks.
  - Adjusted margins and row spacing to reduce overlap 带 32-size labels.
- 结果:
  - Regenerated `figures/03_throughput/throughput_stability_pow.pdf`.
  - Render-checked `/tmp/plot3_pow_4ticks.png`; the figure now uses larger text and denser ticks.
- 下一步:
  - 无
