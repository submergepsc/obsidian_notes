URL: https://www.runoob.com/tags/canvas-lineto.html
Title: HTML canvas lineTo() 方法 | 菜鸟教程

HTML canvas lineTo() 方法 | 菜鸟教程 

菜鸟教程 -- 学的不仅是技术，更是梦想！ 

HTML 参考手册 

HTML canvas closePath() 方法 

HTML canvas clip() 方法 

HTML canvas lineTo() 方法 

实例 

开始一条路径，移动到位置 0,0。创建到达位置 300,150 的一条线： 

JavaScript： 

var c=document.getElementById("myCanvas"); 
var ctx=c.getContext("2d"); 
ctx.beginPath(); 
ctx.moveTo(0,0); 
ctx.lineTo(300,150); 
ctx.stroke(); 

尝试一下 » 

浏览器支持 

Internet Explorer 9、Firefox、Opera、Chrome 和 Safari 支持 lineTo() 
方法。 

注意： Internet Explorer 8 及之前的版本不支持 <canvas> 元素。 

定义和用法 

lineTo() 方法添加一个新点，然后创建从该点到画布中最后指定点的线条（该方法并不会创建线条）。 

提示： 请使用 stroke() 方法在画布上绘制确切的路径。 

JavaScript 语法： 

context .lineTo( x,y ); 

参数值 

参数 

描述 

x 

路径的目标位置的 x 坐标。 

y 

路径的目标位置的 y 坐标。 

更多实例 

实例 

绘制一条路径，形状是字母 L： 

JavaScript： 

var c=document.getElementById("myCanvas"); 
var ctx=c.getContext("2d"); 
ctx.beginPath(); 
ctx.moveTo(20,20); 
ctx.lineTo(20,100); 
ctx.lineTo(70,100); 
ctx.stroke(); 

尝试一下 » 

AI 思考中... 

HTML canvas closePath() 方法 

HTML canvas clip() 方法
